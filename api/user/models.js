const db = require('../../data/dbConfig.js')

const findAll = () => {};

const findById = (id) => {}:

const findByName = (name) => {};

const insert = (user) => {};

const remove = (id) => {};

const update = (id, changes) => {};

module.exports = {
    findAll,
    findById,
    findByName,
    insert, 
    remove,
    update,
}

